import { FondoBotonPipe } from './fondo-boton.pipe';

describe('FondoBotonPipe', () => {
  it('create an instance', () => {
    const pipe = new FondoBotonPipe();
    expect(pipe).toBeTruthy();
  });
});
